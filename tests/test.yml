---
- hosts: all
  gather_facts: yes

  pre_tasks:
    - name: Load test vars
      include_vars: "{{ item }}"
      with_fileglob:
        - vars/*.yml
      tags: [always]

  roles:
    - role: command-line-tools
      tags: [command-line-tools]

    - role: dock
      tags: [dock]
